import Link from "next/link";
import { login } from "@/modules/identity/actions";
export default async function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const { error } = await searchParams; return <div className="auth-card"><div className="auth-brand"><span className="brand-mark">A</span><div><strong>AccordFlow</strong><small>Self-governing sales operations</small></div></div><div className="eyebrow">Welcome back</div><h1>Sign in to your workspace</h1><p className="muted">Use your internal or customer credentials.</p>{error && <div className="alert danger">{error}</div>}<form action={login} className="form-stack"><label>Email<input name="email" type="email" defaultValue="ravi@accordflow.demo" required/></label><label>Password<input name="password" type="password" defaultValue="demo1234" required minLength={8}/></label><button className="button primary">Sign in</button></form><div className="demo-credentials"><strong>Demo access</strong><span>admin@accordflow.demo · manager@accordflow.demo</span><span>finance@accordflow.demo · ravi@accordflow.demo</span><span>Password: demo1234</span></div><p className="auth-foot">New sales rep? <Link href="/signup">Create an account</Link></p></div>; }
