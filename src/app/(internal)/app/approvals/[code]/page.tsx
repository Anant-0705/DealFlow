import Link from "next/link";
import { notFound } from "next/navigation";
import { approve, reject, returnForRevision } from "@/modules/approvals/actions";
import { getQuoteDetail } from "@/modules/quotes/queries";
import { requirePageRole } from "@/lib/auth";
import { APPROVER_ROLES } from "@/lib/roles";
import { formatMoney, formatPercent } from "@/lib/money";
import { ReasonList } from "@/components/shared/ReasonList";
import { StatusBadge } from "@/components/shared/StatusBadge";
import { AuditTrail } from "@/components/approvals/AuditTrail";

export default async function ApprovalReviewPage({ params }: { params: Promise<{ code: string }> }) {
  const session = await requirePageRole(APPROVER_ROLES);
  const { code } = await params; const quote = await getQuoteDetail(code); if (!quote?.currentRevision) notFound(); const revision = quote.currentRevision;
  const expectedLevel = session.role === "FINANCE" ? "FINANCE" : "MANAGER";
  const activeStep = revision.approvalSteps.find((step) => step.level === expectedLevel && step.status === "PENDING" && revision.approvalSteps.filter((prior) => prior.sequence < step.sequence).every((prior) => prior.status === "APPROVED"));
  const reasons = Array.isArray(revision.reasons) ? revision.reasons.filter((item): item is string => typeof item === "string") : [];
  return <div><div className="page-header"><div><Link className="back-link" href="/app/approvals">← Approval inbox</Link><div className="eyebrow">{quote.customer.name} · {quote.customer.tier} · Owner {quote.owner.name}</div><h1>{quote.code} <span>v{revision.version}</span></h1><p>{formatMoney(revision.totalPaise)} total · {formatMoney(revision.marginPaise)} margin ({formatPercent(revision.marginBps, 1)})</p></div><StatusBadge status={quote.approvalStatus}/></div><div className="approval-grid"><div><section className="panel risk-panel"><div className="panel-heading"><div><span className="eyebrow">Explainable governance</span><h2>Why this quote was flagged</h2></div><StatusBadge status={revision.requiredLevel}/></div><ReasonList reasons={reasons}/><div className="risk-metrics"><div><span>Max line excess</span><strong>{formatPercent(revision.maxLineExcessBps, 1)}</strong></div><div><span>Blended excess</span><strong>{formatPercent(revision.blendedExcessBps, 1)}</strong></div><div><span>Excess value</span><strong>{formatMoney(revision.excessValuePaise)}</strong></div></div></section><section className="panel"><div className="panel-heading"><div><span className="eyebrow">Policy detail</span><h2>Quotation lines</h2></div></div><div className="table-scroll"><table><thead><tr><th>Line</th><th>Qty</th><th>Given</th><th>Allowed</th><th>Over by</th><th>Net</th></tr></thead><tbody>{revision.lines.map((line) => <tr className={line.excessBps ? "row-over" : ""} key={line.id}><td>{line.description}</td><td>{line.qty}</td><td>{formatPercent(line.effectiveDiscountBps, 1)}</td><td>{formatPercent(line.allowedDiscountBps, 1)}</td><td>{line.excessBps ? `${line.excessBps / 100} pts` : "OK"}</td><td>{formatMoney(line.netPaise)}</td></tr>)}</tbody></table></div></section><AuditTrail events={quote.auditEvents}/></div><aside><section className="panel step-panel"><span className="eyebrow">Approval path</span><h2>Decision steps</h2><ol><li className="done"><span>✓</span><div><strong>Submitted</strong><small>{revision.submittedAt ? new Date(revision.submittedAt).toLocaleString("en-IN", { timeZone: "Asia/Kolkata" }) : "Draft"}</small></div></li>{revision.approvalSteps.map((step) => <li key={step.id} className={step.status === "APPROVED" ? "done" : step.status === "PENDING" ? "current" : "muted"}><span>{step.status === "APPROVED" ? "✓" : step.sequence}</span><div><strong>{step.level === "MANAGER" ? "Sales Manager" : "Finance"}</strong><small>{step.status}{step.actor ? ` · ${step.actor.name}` : ""}</small></div></li>)}<li className={quote.approvalStatus === "APPROVED" ? "done" : "muted"}><span>{quote.approvalStatus === "APPROVED" ? "✓" : revision.approvalSteps.length + 1}</span><div><strong>Approved</strong><small>Ready for customer flow</small></div></li></ol></section>{activeStep ? <form className="panel decision-form"><span className="eyebrow">Your decision</span><h2>{activeStep.level === "MANAGER" ? "Sales Manager" : "Finance"} review</h2><input type="hidden" name="stepId" value={activeStep.id}/><label>Reason<textarea name="reason" required minLength={3} placeholder="Explain the decision for the audit trail"/></label><div className="decision-actions"><button className="button primary" formAction={approve}>Approve</button><button className="button secondary" formAction={returnForRevision}>Return for revision</button><button className="button danger" formAction={reject}>Reject</button></div></form> : <div className="panel empty-note">There is no actionable {expectedLevel.toLowerCase()} step for this revision.</div>}</aside></div></div>;
}
