/* eslint-disable react-hooks/purity */
import Link from "next/link";
import { ArrowRight, FileCheck2, FileText, ShieldAlert } from "lucide-react";
import { prisma } from "@/lib/prisma";
import { requireInternal } from "@/lib/auth";
import { hasRole, QUOTE_EDITOR_ROLES } from "@/lib/roles";
export default async function DashboardPage({ searchParams }: { searchParams: Promise<{ notice?: string }> }) { const session = await requireInternal(); const canCreate = hasRole(session.role, QUOTE_EDITOR_ROLES); const [{ notice }, pending, open, atRisk] = await Promise.all([searchParams, prisma.quote.count({ where: { approvalStatus: "PENDING" } }), prisma.quote.count({ where: { customerStatus: { in: ["DRAFT", "SENT", "NEGOTIATING"] } } }), prisma.quote.count({ where: { lastActivityAt: { lt: new Date(Date.now() - 5 * 86_400_000) }, customerStatus: { not: "CONFIRMED" } } })]); return <div><div className="page-header"><div><div className="eyebrow">Sales workspace</div><h1>Good afternoon. Keep deals moving.</h1><p>One workspace for pricing discipline, approvals, and accountable execution.</p></div>{canCreate && <Link className="button primary" href="/app/quotations/new">+ New quotation</Link>}</div>{notice && <div className="alert warning">{notice}</div>}<div className="stats-grid"><article><FileCheck2/><span>Pending approvals</span><strong>{pending}</strong><small>Waiting for a decision</small></article><article><FileText/><span>Open quotations</span><strong>{open}</strong><small>Active commercial work</small></article><article><ShieldAlert/><span>At-risk deals</span><strong>{atRisk}</strong><small>Idle more than 5 days</small></article></div><div className="dashboard-grid"><section className="panel"><div className="panel-heading"><div><span className="eyebrow">Phase 1 operating loop</span><h2>From draft to governed approval</h2></div></div><ol className="operating-loop"><li><b>01</b><span><strong>Build</strong><small>Customer pricing, lines, margin and upsells</small></span></li><li><b>02</b><span><strong>Explain</strong><small>Every policy decision is visible before submit</small></span></li><li><b>03</b><span><strong>Approve</strong><small>Manager first, finance only when risk warrants it</small></span></li><li><b>04</b><span><strong>Revise</strong><small>New snapshot, stale approvals, intact history</small></span></li></ol></section><section className="panel action-panel"><span className="eyebrow">Ready to work</span><h2>Open the live deal pipeline</h2><p>Seeded quotes make every stage and approval path visible on first launch.</p><Link href="/app/pipeline">View pipeline <ArrowRight size={16}/></Link></section></div></div>; }
