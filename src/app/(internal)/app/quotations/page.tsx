import Link from "next/link";
import { DataTable } from "@/components/shared/DataTable";
import { listQuotes } from "@/modules/quotes/queries";
import { deriveStage } from "@/modules/quotes/stages";
import { formatMoney } from "@/lib/money";
import { requireInternal } from "@/lib/auth";
import { hasRole, QUOTE_EDITOR_ROLES } from "@/lib/roles";
export default async function QuotationsPage({ searchParams }: { searchParams: Promise<{ status?: string }> }) { const [session, query] = await Promise.all([requireInternal(), searchParams]); const quotes = await listQuotes(session.role === "REP" ? session.userId : undefined); const canCreate = hasRole(session.role, QUOTE_EDITOR_ROLES); const filtered = query.status === "open" ? quotes.filter((quote) => ["DRAFT", "SENT", "NEGOTIATING"].includes(quote.customerStatus) && quote.approvalStatus !== "REJECTED") : query.status === "pending" ? quotes.filter((quote) => quote.approvalStatus === "PENDING") : quotes; const rows = filtered.map((quote) => ({ id: quote.id, href: `/app/quotations/${quote.code}`, code: quote.code, customer: quote.customer.name, amount: formatMoney(quote.currentRevision?.totalPaise ?? 0), stage: deriveStage(quote), risk: quote.currentRevision?.requiredLevel ?? "NONE", revision: `v${quote.currentRevision?.version ?? 1}`, owner: quote.owner.name })); return <div><div className="page-header"><div><div className="eyebrow">Deal core</div><h1>Quotations</h1><p>{query.status === "open" ? "Open commercial records." : query.status === "pending" ? "Quotations waiting for approval." : "All active and historical commercial records."}</p></div><div className="header-actions"><Link className="button secondary" href="/app/pipeline">Switch to Kanban View</Link>{canCreate && <Link className="button primary" href="/app/quotations/new">+ New quotation</Link>}</div></div><DataTable columns={[{ key: "code", label: "Quotation" }, { key: "customer", label: "Customer" }, { key: "amount", label: "Amount" }, { key: "stage", label: "Stage" }, { key: "risk", label: "Approval route" }, { key: "revision", label: "Revision" }, { key: "owner", label: "Owner" }]} rows={rows} empty="No quotations match this view."/></div>; }
