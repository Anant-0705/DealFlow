import Link from "next/link";
import { notFound } from "next/navigation";
import { getProduct } from "@/modules/catalog/queries";
import { saveProduct, saveVariant } from "@/modules/catalog/actions";
import { listPlans } from "@/modules/plans/queries";
import { prisma } from "@/lib/prisma";
export default async function ProductDetailPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const [product, categories, plans] = await Promise.all([getProduct(Number(id)), prisma.category.findMany(), listPlans()]); if (!product) notFound(); return <div><Link href="/app/settings/products" className="back-link">← Products</Link><div className="settings-grid"><form action={saveProduct} className="panel form-stack"><input type="hidden" name="id" value={product.id}/><div><span className="eyebrow">Edit product</span><h2>{product.name}</h2></div><label>Name<input name="name" defaultValue={product.name} required/></label><div className="form-row"><label>SKU<input name="sku" defaultValue={product.sku} required/></label><label>Category<select name="categoryId" defaultValue={product.categoryId}>{categories.map((category) => <option value={category.id} key={category.id}>{category.name}</option>)}</select></label></div><div className="form-row"><label>Unit<input name="unit" defaultValue={product.unit}/></label><label>Tax %<input name="taxPercent" type="number" step="0.01" defaultValue={product.taxBps / 100}/></label></div><div className="form-row"><label>List price ₹<input name="listPriceRupees" type="number" step="0.01" defaultValue={product.listPricePaise / 100}/></label><label>Cost ₹<input name="costRupees" type="number" step="0.01" defaultValue={product.costPaise / 100}/></label></div><label>Description<textarea name="description" defaultValue={product.description}/></label><label className="check"><input type="checkbox" name="isSubscription" defaultChecked={product.isSubscription}/>Subscription product</label><label>Plan<select name="planId" defaultValue={product.planId ?? ""}><option value="">Not recurring</option>{plans.map((plan) => <option value={plan.id} key={plan.id}>{plan.name}</option>)}</select></label><div className="form-row"><label className="check"><input type="checkbox" name="isPromoted" defaultChecked={product.isPromoted}/>Promoted</label><label className="check"><input type="checkbox" name="active" defaultChecked={product.active}/>Active</label></div><button className="button primary">Save product</button></form><section className="panel"><div className="panel-heading"><div><span className="eyebrow">Attribute · value · extra price</span><h2>Variants</h2></div></div><div className="variant-list">{product.variants.map((variant) => <form action={saveVariant} key={variant.id}><input type="hidden" name="id" value={variant.id}/><input type="hidden" name="productId" value={product.id}/><input name="attributeName" defaultValue={variant.attributeName}/><input name="attributeValue" defaultValue={variant.attributeValue}/><input name="extraPriceRupees" type="number" step="0.01" defaultValue={variant.extraPricePaise / 100}/><button className="button secondary small">Save</button></form>)}</div><form action={saveVariant} className="variant-create"><input type="hidden" name="productId" value={product.id}/><input name="attributeName" placeholder="Attribute, e.g. RAM" required/><input name="attributeValue" placeholder="Value, e.g. 64GB" required/><input name="extraPriceRupees" type="number" step="0.01" placeholder="Extra ₹" defaultValue="0"/><button className="button primary small">Add variant</button></form></section></div></div>; }
