import Link from "next/link";
import { saveProduct } from "@/modules/catalog/actions";
import { listProducts } from "@/modules/catalog/queries";
import { listPlans } from "@/modules/plans/queries";
import { prisma } from "@/lib/prisma";
import { formatMoney, formatPercent } from "@/lib/money";
export default async function ProductsSettingsPage() { const [products, categories, plans] = await Promise.all([listProducts(), prisma.category.findMany({ orderBy: { name: "asc" } }), listPlans()]); return <div className="settings-grid"><section className="panel"><div className="panel-heading"><div><span className="eyebrow">Catalog</span><h2>{products.length} products</h2></div></div><div className="table-scroll"><table><thead><tr><th>Product</th><th>Category</th><th>Price</th><th>Cost</th><th>Tax</th><th>Status</th></tr></thead><tbody>{products.map((product) => <tr key={product.id}><td><Link href={`/app/settings/products/${product.id}`}>{product.name}<small>{product.sku} · {product.variants.length} variants</small></Link></td><td>{product.category.name}</td><td>{formatMoney(product.listPricePaise)}</td><td>{formatMoney(product.costPaise)}</td><td>{formatPercent(product.taxBps)}</td><td>{product.active ? "Active" : "Archived"}</td></tr>)}</tbody></table></div></section><form action={saveProduct} className="panel form-stack"><div><span className="eyebrow">Create</span><h2>New product</h2></div><label>Name<input name="name" required/></label><div className="form-row"><label>SKU<input name="sku" required/></label><label>Category<select name="categoryId">{categories.map((category) => <option value={category.id} key={category.id}>{category.name}</option>)}</select></label></div><div className="form-row"><label>Unit<input name="unit" defaultValue="each" required/></label><label>Tax %<input name="taxPercent" type="number" step="0.01" defaultValue="18" min="0" max="100"/></label></div><div className="form-row"><label>List price ₹<input name="listPriceRupees" type="number" step="0.01" min="0.01" required/></label><label>Cost ₹<input name="costRupees" type="number" step="0.01" min="0" required/></label></div><label>Description<textarea name="description" required/></label><label className="check"><input type="checkbox" name="isSubscription"/>Subscription product</label><label>Plan<select name="planId"><option value="">Not recurring</option>{plans.map((plan) => <option value={plan.id} key={plan.id}>{plan.name}</option>)}</select></label><div className="form-row"><label className="check"><input type="checkbox" name="isPromoted"/>Promoted</label><label className="check"><input type="checkbox" name="active" defaultChecked/>Active</label></div><button className="button primary">Create product</button></form></div>; }
