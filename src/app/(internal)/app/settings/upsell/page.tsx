import { listPairings } from "@/modules/upsell/queries";
export default async function UpsellSettingsPage() { const pairings = await listPairings(); return <section className="panel"><div className="panel-heading"><div><span className="eyebrow">Deterministic ranking</span><h2>Seeded product pairings</h2></div></div><p className="muted">Pairing weight × 10, co-purchase history, and promotion status determine the quote builder’s top three. Low-margin products are excluded.</p><div className="table-scroll"><table><thead><tr><th>When quote contains</th><th>Suggest</th><th>Weight</th><th>Promoted</th></tr></thead><tbody>{pairings.map((pairing) => <tr key={pairing.id}><td>{pairing.product.name}</td><td>{pairing.suggestedProduct.name}</td><td>{pairing.weight}</td><td>{pairing.suggestedProduct.isPromoted ? "Yes" : "No"}</td></tr>)}</tbody></table></div></section>; }
