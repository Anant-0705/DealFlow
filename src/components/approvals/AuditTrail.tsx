import { Clock3 } from "lucide-react";
export function AuditTrail({ events }: { events: Array<{ id: number; action: string; at: Date; reason: string | null; actor: { name: string } | null }> }) { return <section className="panel audit-panel"><div className="panel-heading"><div><span className="eyebrow">Immutable record</span><h2>Audit trail</h2></div></div><div className="timeline">{events.map((event) => <article key={event.id}><span className="timeline-dot"><Clock3 size={13}/></span><div><strong>{event.action.replaceAll("_", " ")}</strong><p>{event.reason || "No note supplied"}</p><small>{event.actor?.name ?? "System"} · {event.at.toLocaleString("en-IN", { timeZone: "Asia/Kolkata", dateStyle: "medium", timeStyle: "short" })} IST</small></div></article>)}</div></section>; }
