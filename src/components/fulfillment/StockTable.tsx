export function StockTable({ rows }: { rows: Array<{ id: number; onHand: number; reserved: number; available: number; reorderPoint: number; reorderQty: number; maxOnHand: number; warehouse: { name: string }; product: { name: string }; variant: { attributeValue: string } | null }> }) {
  return <div className="table-scroll"><table><thead><tr><th>Warehouse</th><th>Product / variant</th><th>On hand</th><th>Reserved</th><th>Available</th><th>Reorder</th></tr></thead><tbody>{rows.map((row) => <tr key={row.id}><td>{row.warehouse.name}</td><td>{row.product.name}<small>{row.variant?.attributeValue ?? "Base"}</small></td><td>{row.onHand}</td><td>{row.reserved}</td><td><strong>{row.available}</strong></td><td>{row.reorderPoint > 0 ? `${row.reorderPoint} / +${row.reorderQty}${row.maxOnHand ? ` · max ${row.maxOnHand}` : ""}` : "—"}</td></tr>)}</tbody></table></div>;
}
