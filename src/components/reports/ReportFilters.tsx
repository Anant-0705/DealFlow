import type { ReportFilters } from "@/modules/reports/filters";

type Options = { reps: Array<{ id: number; name: string }>; categories: Array<{ id: number; name: string }>; products: Array<{ id: number; name: string; categoryId: number }> };

export function ReportFiltersForm({ filters, options, lockRep }: { filters: ReportFilters; options: Options; lockRep: boolean }) {
  const products = filters.categoryId ? options.products.filter((product) => product.categoryId === filters.categoryId) : options.products;
  return <form className="report-filters" method="get"><label>View<select name="mode" defaultValue={filters.mode}><option value="quotations">Quotations</option><option value="orders">Orders</option></select></label><label>Period<select name="period" defaultValue={filters.period}><option value="today">Today</option><option value="week">This week</option><option value="month">This month</option><option value="custom">Custom</option></select></label><label>From<input name="from" type="date" defaultValue={filters.from}/></label><label>To<input name="to" type="date" defaultValue={filters.to}/></label><label>Sales rep<select name="repId" defaultValue={filters.repId ?? ""} disabled={lockRep}><option value="">All</option>{options.reps.map((rep) => <option key={rep.id} value={rep.id}>{rep.name}</option>)}</select></label><label>Approval<select name="approvalStatus" defaultValue={filters.approvalStatus}><option value="all">All</option><option value="auto">Auto-approved</option><option value="pending">Pending</option><option value="approved">Approved</option><option value="rejected">Rejected</option><option value="returned">Returned</option></select></label><label>Category<select name="categoryId" defaultValue={filters.categoryId ?? ""}><option value="">All</option>{options.categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></label><label>Product<select name="productId" defaultValue={filters.productId ?? ""}><option value="">All</option>{products.map((product) => <option key={product.id} value={product.id}>{product.name}</option>)}</select></label><button className="button primary" type="submit">Apply filters</button></form>;
}
